INSERT INTO public.licenses (wallet, plan, activated_at, expires_at, created_at)
VALUES (
  'PASTE_USER_WALLET_HERE',
  'month',
  NOW(),
  NOW() + INTERVAL '30 days',
  NOW()
)
ON CONFLICT (wallet) 
DO UPDATE SET 
  plan = 'month',
  activated_at = NOW(),
  expires_at = NOW() + INTERVAL '30 days',
  device_id = NULL,
  session_token = NULL;